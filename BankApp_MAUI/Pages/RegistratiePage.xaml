<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BankApp_MAUI.ViewModels"
             x:Class="BankApp_MAUI.Pages.RegistratiePage"
             x:DataType="vm:RegistratieViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource BackgroundBlack}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo -->
            <Label Text="AccessBank"
                   FontSize="48"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="White"
                   Margin="0,0,0,40"/>

            <!-- Card voor registratieformulier -->
            <Frame BackgroundColor="White"
                   CornerRadius="10"
                   Padding="20"
                   HasShadow="True">

                <VerticalStackLayout Spacing="15">
                    <Label Text="Account aanmaken"
                           FontSize="24"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <!-- Voornaam -->
                    <Label Text="Voornaam"/>
                    <Entry Text="{Binding Voornaam}"
                           Placeholder="Voornaam"
                           BackgroundColor="{StaticResource InputGray}"
                           TextColor="White"/>

                    <!-- Achternaam -->
                    <Label Text="Achternaam"/>
                    <Entry Text="{Binding Achternaam}"
                           Placeholder="Achternaam"
                           BackgroundColor="{StaticResource InputGray}"
                           TextColor="White"/>

                    <!-- Email -->
                    <Label Text="E-mail"/>
                    <Entry Text="{Binding Email}"
                           Placeholder="voorbeeld@email.com"
                           Keyboard="Email"
                           BackgroundColor="{StaticResource InputGray}"
                           TextColor="White"/>

                    <!-- Wachtwoord -->
                    <Label Text="Wachtwoord"/>
                    <Entry Text="{Binding Wachtwoord}"
                           IsPassword="True"
                           Placeholder="********"
                           BackgroundColor="{StaticResource InputGray}"
                           TextColor="White"/>

                    <!-- Wachtwoord bevestigen -->
                    <Label Text="Wachtwoord bevestigen"/>
                    <Entry Text="{Binding WachtwoordBevestiging}"
                           IsPassword="True"
                           Placeholder="********"
                           BackgroundColor="{StaticResource InputGray}"
                           TextColor="White"/>

                    <!-- Error message -->
                    <Label Text="{Binding ErrorMessage}"
                           TextColor="Red"
                           IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
                           FontSize="12"/>

                    <!-- Registreer button -->
                    <Button Text="Registreren"
                            Command="{Binding RegistreerCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                            BackgroundColor="{StaticResource PrimaryGreen}"
                            TextColor="White"
                            Margin="0,10,0,0"/>

                    <!-- Loading indicator -->
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                     IsVisible="{Binding IsBusy}"
                                     Color="{StaticResource PrimaryGreen}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Link terug naar login -->
            <Label Text="Heb je al een account? Log hier in"
                   TextColor="White"
                   FontSize="14"
                   HorizontalOptions="Center"
                   Margin="0,20,0,0">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding GaNaarLoginCommand}"/>
                </Label.GestureRecognizers>
            </Label>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
