<UserControl x:Class="BankApp_WPF.CustomControls.SaldoCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Border Background="#1E3A5F"
            CornerRadius="20"
            Padding="30"
            Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0"
                       Text="{Binding Type, StringFormat='{}{0} Rekening'}"
                       FontSize="18"
                       Foreground="#B0C4DE"
                       Margin="0,0,0,10"/>
            
            <TextBlock Grid.Row="1"
                       Text="{Binding Iban}"
                       FontSize="16"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="Saldo: "
                           FontSize="20"
                           Foreground="#B0C4DE"/>
                <TextBlock Text="{Binding Saldo, StringFormat='€ {0:F2}'}"
                           FontSize="24"
                           Foreground="#FFD700"
                           FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

